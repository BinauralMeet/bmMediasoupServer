// Placeholder for shared vitest setup, added per-file as tests uncover what they need
// (e.g. GoogleServer.ts and utils.ts have module-load-time side effects that need config/fs
// stubbing outside a running server -- see individual test files for what each needs).
export {}
